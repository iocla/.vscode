{
	"tasks": [
		{
			"label": "make all",
			"type": "shell",
			"command": "make",
			"args": [
				"all",
				""
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		},
		{
			"label": "make clean",
			"type": "shell",
			"command": "make",
			"args": [
				"clean",
				""
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"group": {
				"kind": "build",
				"isDefault": false
			}
		}
	]
}